<h1>Artists</h1>
<br>
<br>
<ul>
  <% @artists.each do |artist| %>
    <li><h3><%= link_to "#{artist.name}", artist_path(artist) %> - <%= pluralize(artist.song_count, 'song') %></<%= artist.song_count %></h3></li>
  <% end %>
</ul>
